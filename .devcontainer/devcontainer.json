{  
    // Install ESLint and Peacock extensions
    "extensions": [
      "ms-vscode.cmake-tools",
      "ms-vscode.cpptools"
    ],

    // Run Bash script in .devcontainer directory
    "postCreateCommand": "sudo /bin/bash ./.devcontainer/env.sh"
}